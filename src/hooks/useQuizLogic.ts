import React from 'react';

function useQuizLogic() {
  const startTrivia = async () => {};

  const checkAnswer = (e: React.MouseEvent<HTMLButtonElement>) => {};

  const nextQuestion = () => {};
  return { startTrivia, checkAnswer, nextQuestion };
}

export default useQuizLogic;
